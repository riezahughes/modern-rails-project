<%= form_for(@client_file_information) do |f| %>
    <% if @client_file_information.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@client_file_information.errors.count, "error") %> prohibited this client_file_information
            from being saved:</h2>

          <ul>
            <% @client_file_information.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="row">
      <div class="col-md-4">

        <div class="actions form-group">
          <%= f.submit_change_aware %>
        </div>

        <div class="field form-group">
          <%= f.label :name, class: "control-label" %><br>
          <%= f.text_field :name, class: "form-control" %>
        </div>

      </div>
    </div>
<% end %>
